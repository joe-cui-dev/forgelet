{
  "name": "forgelet",
  "version": "0.1.0",
  "description": "Local-first CLI agent workbench for coding, writing, learning, and knowledge workflows.",
  "type": "module",
  "bin": {
    "forge": "./dist/cli/index.js"
  },
  "files": [
    "dist",
    "fixtures",
    "README.md",
    "AGENTS.md",
    "ROADMAP.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli/index.js && node dist/browser/extension/buildExtension.js",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand",
    "smoke:deepseek": "npm run build && node dist/models/providers/deepseekSmoke.js",
    "smoke:writing": "npm run build && node dist/smoke/writingCreative.js",
    "smoke:writing-artifacts": "npm run build && node dist/smoke/writingArtifacts.js",
    "smoke:learning": "npm run build && node dist/smoke/learning.js",
    "smoke:learning-web": "npm run build && node dist/smoke/learningWeb.js",
    "smoke:knowledge-notes": "npm run build && node dist/smoke/knowledgeNotes.js",
    "smoke:memory-review": "npm run build && node dist/smoke/memoryReview.js",
    "smoke:browser-workbench": "npm run build && node dist/smoke/browserWorkbench.js",
    "clean": "rm -rf dist dist-test"
  },
  "keywords": [
    "agent",
    "cli",
    "agent-workbench",
    "local-first"
  ],
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.13.2",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^5.9.3"
  }
}
