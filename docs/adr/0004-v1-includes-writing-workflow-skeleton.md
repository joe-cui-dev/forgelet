# V1 Includes a Writing Workflow Skeleton

Forgelet V1 will make the coding workflow the first fully usable path and include a lightweight writing workflow skeleton to validate that the Agent Kernel is not coding-specific. The writing workflow should stay text-first in V1 and avoid document editor, browser automation, image, or publishing integrations until the kernel, permission, trace, budget, and model-routing boundaries are proven.
